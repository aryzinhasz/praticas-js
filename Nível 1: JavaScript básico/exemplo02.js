//Interagindo com um botão

const button = document.getElementById("meuBotao");
button.addEventListener("click", function () {
  alert("Botão clicado!");
});

//Para testar, crie um arquivo HTML com um botão que tenha o id "meuBotao" e vincule este script a ele.

//Esse código adiciona um evento de clique a um botão no HTML, que exibe um alerta quando clicado.
