//Contador de cliques

let contador = 0;
function contar() {
  contador++;
  document.getElementById("resultado").innerText = "Cliques: " + contador;
}

//Esse código implementa um contador de cliques. Cada vez que o botão é clicado, a função contar() é chamada, incrementando o contador e atualizando o texto exibido na página HTML.
