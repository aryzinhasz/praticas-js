//Interagindo com partes do HTML (DOM)

function mudarTexto() {
  document.getElementById("titulo").innerText = "O texto foi alterado!";
}

//Esse código altera o texto de um elemento HTML com o id "titulo" quando a função mudarTexto é chamada.
